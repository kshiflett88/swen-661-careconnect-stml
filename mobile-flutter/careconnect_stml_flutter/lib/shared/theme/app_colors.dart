import 'package:flutter/material.dart';

class AppColors {
  // Text
  static const Color textPrimary = Color(0xFF0F172B);
  static const Color textSecondary = Color(0xFF45556C);

  // Surfaces
  static const Color surface = Colors.white;
  static const Color infoCardBg = Color(0xFFEFF6FF);

  // Borders
  static const Color infoCardBorder = Color(0xFFBEDBFF);
  static const Color outlineBorder = Color(0xFFCAD5E2);

  // Primary action
  static const Color primary = Color(0xFF155DFC);
}
